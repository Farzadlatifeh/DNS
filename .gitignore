```gitignore
# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/
.ENV
.python-version

# Logs
*.log

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Local configuration
.env
.env.local
*.env
*.env.*
```

Wait, let me reconsider. The file changes only include `dns_profiles.json` and `extract_dns.py`, which are both source/config files. There are no build artifacts, dependencies, or temp files in the change list.

(empty)